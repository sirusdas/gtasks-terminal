name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: "context-llemur"
    # Use the absolute path to the executable here
    command: "/Users/int/.pyenv/versions/3.11.0/bin/ctx"
    args: ["mcp"]
